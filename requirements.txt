# networkx is THIS repo's dependency, never heteropilot's (D123). Its version is
# recorded in `Signature.tool_version` because a Weisfeiler-Lehman hash is only
# comparable against itself: the same graph hashed by two releases may differ,
# and an equivalence class silently re-cut by an upgrade would be invisible.
networkx>=3.2

# What vendor/heteropilot's planner/ needs to be imported and run against a
# mock predictor.
pydantic
pyyaml
numpy

# Reached through `planner.util.memory`, which calls into upstream `serving/`:
#   rich     - serving.core.logger, via memory_model -> radix_tree
#   msgspec  - serving.core.radix_tree. It looks optional there (an ImportError
#              sets it to None) but the module then subclasses `msgspec.Struct`
#              unconditionally, so the guard does not work and it is required.
# Not needed before G3; `demand.py` is the first module here to size a KV
# transfer, which is what reaches that far.
rich
msgspec

# `planner.predictor.llmservingsim` imports pandas, and `adapter.py` imports
# that module for `compile_to_sim_config` and the compile hook. Not needed
# before G11 -- which is why the comment here used to say the opposite.
#
# Running the real simulator needs far more than any of this; reuse
# `vendor/heteropilot/.venv` for that (see README).
pandas

pytest
ruff
mypy
